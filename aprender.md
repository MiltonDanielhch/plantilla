# 📚 Guía de Aprendizaje para el Proyecto Sintonía 3026

Esta guía te proporciona una hoja de ruta para adquirir los conocimientos necesarios para contribuir y dominar el proyecto, desde lo más básico hasta lo más avanzado.

## 1. Fundamentos (Base Sólida)

*   **Rust:**
    *   Sintaxis básica (variables, tipos de datos, funciones, estructuras de control).
    *   Ownership y Borrowing (conceptos clave de Rust para seguridad de memoria).
    *   Traits (interfaces), generics y lifetimes.
    *   Manejo de errores (`Result`, `Option`, `panic`).
    *   Crates esenciales: `std`, `collections`, `io`.
*   **SQL:**
    *   Consultas básicas (SELECT, INSERT, UPDATE, DELETE).
    *   Cláusulas WHERE, ORDER BY, GROUP BY, JOIN.
    *   Índices y optimización de consultas.
*   **HTML, CSS y JavaScript:**
    *   Estructura básica de documentos web.
    *   Selectores CSS, modelo de caja, Flexbox y Grid.
    *   Manipulación del DOM con JavaScript.
*   **Git:**
    *   Comandos básicos (clone, add, commit, push, pull, branch, merge).
    *   Flujos de trabajo comunes (ej: Gitflow).

## 2. Intermedio (Productividad)

*   **Axum (Rust):**
    *   Conceptos de routing, handlers y middleware.
    *   Extracción de parámetros (Path, Query, State, Json).
    *   Manejo de cookies y sesiones.
    *   Testing de endpoints.
*   **SQLx (Rust):**
    *   Conexión a bases de datos (SQLite, PostgreSQL).
    *   Ejecución de queries y mapeo a structs.
    *   Migraciones de base de datos.
*   **Astro:**
    *   Componentes, layouts y páginas.
    *   Integración con frameworks UI (React, Vue).
    *   Server-Side Rendering (SSR) y Client-Side Rendering (CSR).
    *   Rutas dinámicas.
*   **Testing:**
    *   Creación de pruebas unitarias en Rust (`#[test]`).
    *   Tests de integración (verificar la interacción entre componentes).
    *   Tests E2E (simular el comportamiento del usuario).

## 3. Avanzado (Dominio Total)

*   **Arquitectura Hexagonal (Clean Architecture):**
    *   Separación de responsabilidades (Core, API, Data).
    *   Inversión de dependencias.
*   **Patrones de Diseño:**
    *   Repository Pattern (abstracción de acceso a datos).
    *   Factory Pattern (creación de objetos complejos).
    *   Middleware Pattern (interceptar y procesar peticiones).
*   **Seguridad Avanzada:**
    *   JWT (JSON Web Tokens): Estructura, firma, verificación y mejores prácticas.
    *   RBAC (Role-Based Access Control): Implementación y gestión de roles.
    *   OWASP Top 10: Conocer y prevenir vulnerabilidades comunes.
*   **Performance y Escalabilidad:**
    *   Rate Limiting: Proteger el sistema de ataques.
    *   Caching: Estrategias para reducir la carga en el servidor.
    *   Balanceo de carga.
*   **DevOps:**
    *   Docker y Docker Compose: Contenerización de aplicaciones.
    *   CI/CD (GitHub Actions): Automatización de despliegues.
    *   Monitorización: Sentry, Prometheus, Grafana.
*  **GraphQL** Alternativa a REST
*  **gRPC** Alternativa a REST