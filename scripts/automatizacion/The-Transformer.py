# 6. El Script de "Conversor de Formatos" (The Transformer)
# A veces necesitas pasar datos de un CSV a un JSON, o de una base de datos SQL a un archivo Markdown para un reporte.

# Misión: Un script "navaja suiza" que detecta el formato de entrada y lo transforma al que necesites para tu siguiente fase.