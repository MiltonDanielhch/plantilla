---
---
<div class="user-card">
  <h2>🚀 Registro Sintonía</h2>
  <form id="registerForm">
    <div class="input-group">
      <label for="username">Nombre de Usuario</label>
      <input type="text" id="username" name="username" placeholder="Ej. Neo" required />
    </div>
    <button type="submit">Conectar a la Matrix</button>
  </form>
  <p id="statusMessage"></p>
</div>

<script>
  const form = document.getElementById('registerForm');
  const message = document.getElementById('statusMessage');

  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const formData = new FormData(e.target as HTMLFormElement);
      const username = formData.get('username');

      try {
        const response = await fetch('http://localhost:3000/users', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ username }),
        });

        if (response.ok) {
          const data = await response.json();
          message.textContent = `✅ Usuario creado: ${data.username} (ID: ${data.id})`;
          message.style.color = '#4ade80';
        } else {
          message.textContent = '❌ Error al crear usuario';
          message.style.color = '#f87171';
        }
      } catch (error) {
        console.error(error);
        message.textContent = '❌ Error de conexión con el Núcleo';
        message.style.color = '#f87171';
      }
    });
  }
</script>

<style>
  .user-card { background: #1f2937; color: white; padding: 2rem; border-radius: 1rem; max-width: 400px; margin: 2rem auto; font-family: system-ui; }
  input { width: 100%; padding: 0.5rem; margin: 0.5rem 0 1rem; border-radius: 0.25rem; border: none; }
  button { background: #6366f1; color: white; border: none; padding: 0.75rem; width: 100%; border-radius: 0.25rem; cursor: pointer; font-weight: bold; }
  button:hover { background: #4f46e5; }
</style>