---
import '../styles/globals.css';
import { Button } from "../components/ui/button";
import { ServerCrash } from "lucide-react";
---

<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>500 - Error del Servidor | Sintonía 3026</title>
    <link href="/favicon.svg" rel="icon" type="image/svg+xml" />
</head>
<body class="bg-background text-foreground min-h-screen flex flex-col items-center justify-center p-4 text-center">
    <div class="space-y-6 max-w-md animate-in fade-in zoom-in duration-500">
        <div class="flex justify-center">
            <div class="h-24 w-24 rounded-full bg-destructive/10 flex items-center justify-center ring-8 ring-destructive/5">
                <ServerCrash class="h-12 w-12 text-destructive" />
            </div>
        </div>
        
        <div class="space-y-2">
            <h1 class="text-4xl font-bold tracking-tight lg:text-5xl">500</h1>
            <h2 class="text-xl font-semibold tracking-tight">Error Interno del Servidor</h2>
            <p class="text-muted-foreground">
                Algo salió mal en nuestros sistemas. No es tu culpa. Por favor, intenta recargar la página en unos momentos.
            </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-3 justify-center pt-4">
            <Button size="lg" className="w-full sm:w-auto" onclick="window.location.reload()">
                Recargar Página
            </Button>
            <a href="/dashboard">
                <Button variant="outline" size="lg" className="w-full sm:w-auto">Ir al Dashboard</Button>
            </a>
        </div>
    </div>
</body>
</html>